CREATE TABLE actividades (
                             id BIGINT PRIMARY KEY NOT NULL,
                             actividad VARCHAR(255),
                             descripcion VARCHAR(255),
                             puntaje INT
);
